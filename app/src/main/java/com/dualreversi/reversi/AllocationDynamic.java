package com.dualreversi.reversi;

public class AllocationDynamic extends MainActivity {

    // get dp
    private float dp = getResources().getDisplayMetrics().density;

    // SW button
    public int widthBG       = (int) (360 * dp);
    public int heightBG      = (int) (360 * dp);
    public int bottomBG = (int) (100 * dp);
    public int leftBG   = (int) (0 * dp);
}
